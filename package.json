{
  "name": "firebase-proxy",
  "version": "1.0.0",
  "description": "Secure HTTP proxy with CORS, authentication, and domain whitelisting",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-functions": "^4.0.0",
    "firebase-tools": "^14.25.0",
    "node-fetch": "^2.6.7"
  }
}
